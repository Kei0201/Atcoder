#include <bits/stdc++.h>
using namespace std;

int main () {
    int n, m;   
    cin >> n >> m;
    int a[m], b[m];
    for (int i = 0; i < m; i++) {
        cin >> a[i] >> b[i];
    }

    // 問題の内容は理解している
    // 2024/08/01
}