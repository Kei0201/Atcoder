#include <bits/stdc++.h>
using namespace std;

int main () {
    // 1は真を表すのでhelloと出力
    if (1) {
        cout << "hello" << endl;
    }

    // 0は偽を表すのでこのifの中は実行されない
    if (0) {
        cout << "world" << endl;
    }
}